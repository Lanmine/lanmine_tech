---
# Deploy Open WebUI (local LLM chat interface)
# Replaces the old panda9000 voice assistant
- name: Deploy Open WebUI
  hosts: panda9000_servers
  gather_facts: true
  roles:
    - panda9000_install
    - rsyslog_forward
